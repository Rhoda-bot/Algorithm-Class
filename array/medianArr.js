function medianOfArray(arr, arr2) {
    let firstIndex = 0;
    let lastIndex = arr.length -1
    let median = Math.floor((firstIndex + lastIndex)/ 2)
    for (let i = 0; i < arr.length; i++) {
        const element = arr[i];
        
    }
   
    
}

console.log(medianOfArray([1, 2, 3, 4, 5,6,7,8], [8,7, 6]));