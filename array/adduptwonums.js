const adduptwo = (arr, givenNum) => {
    let result;
    // for (let i = 0; i < arr.length; i++) {
    //     const element = arr[i];
    //     for (let b = 0; b < arr.length; b++) {
    //         const el = arr[b];
    //         if (element + el === givenNum) {
    //          result = element +' '+ el;
    //         }
           
    //     }
    // }
    // return result;
    for (let i = 0; i < arr.length; i++) {
        const element = arr[i];
        
    }
}
console.log(adduptwo([1, 2, 3, 4, 5], 3));